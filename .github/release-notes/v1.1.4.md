## ✨ 新增

- 新增按设备 ABI 匹配更新安装包，优先下载 arm64-v8a、armeabi-v7a、x86_64 或 x86 对应版本，缺失时再使用 universal 包。
- 新增 README 界面预览动图，按卡片墙、设置、添加卡片展示主要界面。

## 🐛 修复

- 修复本地 release 构建的 keystore 路径解析，`.env` 中的相对路径现在按项目根目录解析。

## 🔧 优化

- 优化检查更新网络路径：设置代理时通过代理访问 GitHub，未设置代理时使用 gh.lessdo.top 镜像检查和下载。
- 更新 release keystore 生成脚本和 `.env.example`，补齐本地签名文件路径配置。
